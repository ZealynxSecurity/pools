{
  "name": "pools-contracts",
  "version": "0.0.0",
  "description": "FEVM contracts for GLIF Pools",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/glif-confidential/pools.git"
  },
  "scripts": {
    "build": "run-p build:*",
    "build:types": "mkdir -p typechain && typechain --target=ethers-v5 ./out/**/*[^.metadata].json --out-dir typechain/ --show-stack-traces",
    "build-digest": "mkdir -p generated && yarn clean:digest && touch generated/contractDigest.json && ts-node utils/digestContracts.ts",
    "build:abigen": "mkdir -p generated && ts-node utils/preProcessAbigen.ts",
    "clean": "run-p clean:*",
    "clean:broadcast": "rimraf broadcast",
    "clean:contracts": "rimraf out",
    "clean:digest": "rimraf generated/contractDigest.json",
    "clean:types": "rimraf typechain",
    "start:node": "anvil --block-time 1",
    "test": "forge test -vvv",
    "test:watch": "forge test -w -vvv",
    "test:contract": "forge test -vvv --match-contract",
    "test:watch:contract": "forge test -w -vvv --match-contract"
  },
  "devDependencies": {
    "@typechain/ethers-v5": "^10.1.0",
    "@types/node": "^18.7.21",
    "cross-env": "^7.0.3",
    "npm-run-all2": "^6.0.2",
    "path": "^0.12.7",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typechain": "^8.1.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@ethersproject/providers": "^5.6.8",
    "ethers": "^5.7.1"
  },
  "files": [
    "typechain"
  ]
}
